# Qwen3-32B LoRA SFT 依赖
# 安装命令: pip install -r requirements.txt

# 核心训练框架
torch>=2.1.0
transformers>=4.51.0        # Qwen3 支持需要 4.51+
trl>=0.12.0
peft>=0.14.0
datasets>=2.18.0
accelerate>=0.28.0

# 量化
bitsandbytes>=0.43.0

# Flash Attention（强烈推荐，显著降低显存并提速）
# 需要单独安装: pip install flash-attn --no-build-isolation
# flash-attn>=2.5.0

# 可选：分布式训练
# deepspeed>=0.14.0

# 工具
sentencepiece
einops
tensorboard
